export default function Loading() {
  return (
    <main className="mx-auto flex min-h-screen max-w-[1180px] items-center px-4 text-sm text-[var(--text-muted)] sm:px-6 lg:px-8">
      Loading LDS website content...
    </main>
  );
}
